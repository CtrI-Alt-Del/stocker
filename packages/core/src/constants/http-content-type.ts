import type { HttpContentType } from '../types'

export const HTTP_CONTENT_TYPE: Record<HttpContentType, string> = {
  image: 'image/jpeg',
  csv: 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet',
}
