import { useNavigation } from '../../../hooks'
import { useProductsPage } from './use-producst-page'

export const ProductsPage = () => {
  return <div>{/* <button onClick={() => navigateTo('/products')}></button> */}</div>
}
